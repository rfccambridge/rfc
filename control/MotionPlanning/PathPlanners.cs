using System;
using System.Collections.Generic;
using System.Text;

using Robocup.Core;
using Robocup.CoreRobotics;
using Robocup.Geometry;

using System.Drawing;

using Navigation.Examples;
using System.IO;
using Robocup.Utilities;

namespace Robocup.MotionControl
{
#if FALSE
    public class RRTPathPlanner : IPathPlanner
    {
        public RRTPathPlanner() {

        }
        
        RobotPath GetPath(int id, RobotInfo desiredState, IPredictor predictor, double avoidBallRadius) {
            return new RobotPath();
        }

        void DrawLast(Graphics g, ICoordinateConverter c)
        {

        }
        
        void ReloadConstants() { 
        
        }
    }
#endif
}