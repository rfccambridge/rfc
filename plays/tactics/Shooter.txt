Metadata:
type tactic
name Shooter

Parameters:

Objects:
ballpoint (pointof ball)
ballRobot (closest friendly ballpoint loose)

forbiddenCircle (circle (theirgoal) 0.5)

topPost (point (getXcoord (theirgoal)) 0.35)
bottomPost (point (getXcoord (theirgoal)) (- 0 0.35))

pointKickTop (point (- (getXcoord topPost) 0.5) (getYcoord topPost))
pointkickBottom (point (- (getXcoord bottomPost) 0.5) (getYcoord bottomPost))

midline (line (theirgoal) (ourgoal))

Actions:

# check if we shouldn't go after the ball- that is, if it is
# in the goal and there is another robot there

(if_Action (pointAboveLine ballpoint midline) (robotpointkick ballRobot pointkickBottom) (robotpointkick ballRobot pointKickTop))