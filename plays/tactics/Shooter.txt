Metadata:
type tactic
name Shooter

Parameters:
bot PlayRobotDefinition

Objects:
ballpoint (pointof ball)
standbypoint (point -2 0)

Actions:

# check if we shouldn't go after the ball- that is, if it is
# in the goal and there is another robot there

(if_Action (not (and (pointInGoal ballpoint) (robotInGoal))) (robotpointkick bot (theirgoal)) (robotpointmove bot standbypoint))

